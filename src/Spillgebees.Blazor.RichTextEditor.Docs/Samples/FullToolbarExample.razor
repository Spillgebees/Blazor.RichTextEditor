<RichTextEditor @bind-Content="_content"
                ToolbarOptions="ToolbarOptions.FullToolbarOptions" />

@code {
    private string _content = "<p>The full preset exposes every built-in formatting control.</p>";
}
